// Bumped by hand alongside package.json's "version" field on every change —
// keep both in sync (see the footer in HomePage.tsx for where this is shown).
export const APP_VERSION = '2.0.2';
export const APP_RELEASE_DATE = '20-07-2026';
